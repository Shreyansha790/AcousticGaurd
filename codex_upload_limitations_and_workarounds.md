# Codex Upload Limitation: Why HTML/PDF may not appear in chat upload

## What you are seeing
If your Codex chat UI only allows image uploads (JPEG/PNG), that is a **chat attachment UI limitation**, not a limitation of coding itself.

You can still work on HTML/CSS/JS in Codex.

## What to do instead (best options)

## Option 1 (Best): Put files in the repo/workspace
1. Copy your prototype files (`.html`, `.css`, `.js`) into the project folder.
2. Tell me the file paths.
3. I will read/edit them directly.

## Option 2: Paste the HTML directly in chat
- Paste the content inside triple backticks:

```html
<!-- your file content here -->
```

## Option 3: Share text in chunks
- If your file is large, send it in parts (Part 1/3, 2/3, 3/3).

## Option 4: If only screenshot is available
- Share a screenshot + section names and I can recreate/edit the HTML structure with you.

---

## Fastest path for us right now
Since you already have a prototype, do this:
1. Place your `index.html` (and related CSS/JS) in the repo folder.
2. Send me the relative path (example: `web/index.html`).
3. I will start making exact edits immediately.

---

## Important clarification
- Upload restriction in chat does **not** block development.
- We can still fully build and modify your HTML in Codex by editing files in the workspace.
